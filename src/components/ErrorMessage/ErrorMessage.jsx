import React from "react";

const ErrorMessage = ({ message }) => {
  return (
    <div>
      <p>Ooops... Something gone wrong. Pleasy try again Later!</p>
    </div>
  );
};

export default ErrorMessage;
